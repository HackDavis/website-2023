import Countdown from '@/components/doe/countdown';

const Timer = () => (
  <>
    <div style={{ backgroundColor: 'var(--color-teal-3)' }}>
      <Countdown />
    </div>
  </>
);

export default Timer;
